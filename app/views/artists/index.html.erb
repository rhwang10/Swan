<h1 class="page-header">Results <%= params[:artist_name] %></h1>

<ul>
  <% @artists.each do |artist| %>
  <li><%= artist.name %> | <%= link_to "Show", artist_path(artist.id) %></li>
  <% end %>
</ul>
