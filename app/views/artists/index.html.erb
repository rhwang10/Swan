<h1 class="page-header">Results for "<%= params[:artist_name] %>"</h1>
<h2><%= @num_artist_results %></h2>
<ul>
  <% @artists.each do |artist| %>
  <li><%= artist.name %> | <%= link_to "Show", artist_path(artist.id) %></li>
  <% end %>
</ul>
